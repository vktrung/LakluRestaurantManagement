spring:
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost:3306/pos
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
jwt:
  secret:
