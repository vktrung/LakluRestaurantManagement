server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/Laklu"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-Auto: update
    show-sql: true